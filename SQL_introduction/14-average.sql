-- Task: Compute the average score of all records in 'second_table'

-- This query calculates the average score and names the result column as 'average'
SELECT AVG(score) AS average
FROM second_table;

