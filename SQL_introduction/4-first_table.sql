-- Task 4: Create table 'first_table' if it does not exist
-- The table will have columns 'id' (INT) and 'name' (VARCHAR 256)
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);

