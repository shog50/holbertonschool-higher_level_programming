-- Task: List all records from 'second_table' ordered by score (highest first)

-- This query selects score and name from second_table, ordered by score descending
SELECT score, name
FROM second_table
ORDER BY score DESC;

